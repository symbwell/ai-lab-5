services:
  php:
    image: php:8.4-fpm
    container_name: php_lab
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./app:/var/www/html
      - ./app/config/xdebug.ini:/usr/local/etc/php/conf.d/xdebug-custom.ini
    ports:
      - "33464:33464"